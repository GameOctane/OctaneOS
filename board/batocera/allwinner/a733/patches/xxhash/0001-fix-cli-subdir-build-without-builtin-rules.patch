--- a/Makefile	2026-04-06 11:24:35.213937332 -0700
+++ b/Makefile	2026-04-06 11:24:35.257936904 -0700
@@ -122,6 +122,11 @@
     xxhash.h xxh_x86dispatch.h
 xxh_x86dispatch.o: xxh_x86dispatch.c xxh_x86dispatch.h xxhash.h
 
+# Explicit rule for CLI objects; required when --no-builtin-rules is active
+# (Buildroot propagates it via MAKEFLAGS to all sub-makes).
+cli/%.o: cli/%.c
+	$(CC) $(FLAGS) -I$(XXHSUM_SRC_DIR) -c $< -o $@
+
 .PHONY: xxhsum_and_links
 xxhsum_and_links: xxhsum xxh32sum xxh64sum xxh128sum
 
